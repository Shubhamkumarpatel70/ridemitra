{
  "name": "ride-mitra",
  "version": "1.0.0",
  "private": true,
  "description": "Ride Mitra - full stack (frontend + backend) single deploy",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  }
}
